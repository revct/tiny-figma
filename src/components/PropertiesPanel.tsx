import * as React from "react"
import {HasDispatch} from "../helpers/redux_helpers";

export interface PropertiesPanelProps extends HasDispatch {

}

export const PropertiesPanel = (props: PropertiesPanelProps) =>
  <div>
    Properties Panel
  </div>
